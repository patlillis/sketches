export const DEBUG = true;

// Audio constants.
export const BPM = 95;

// UI constants.
export const ui = {
  PADDING: 30,
  SIZE: 50,
  THICKNESS: 5,
};

// Circle constants.
export const circle = {
  CIRCLE_COUNT: 5,
  MIN_SPEED: 0.5,
  MAX_SPEED: 1.0,
  MIN_EDGE_PADDING: 100,
  MAX_EDGE_PADDING: 300,
};
